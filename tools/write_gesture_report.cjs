const fs=require('node:fs'),words=require('../data/words.json').words,held=require('../gesture-data/evaluation-heldout.json'),loso=require('../gesture-data/evaluation-loso.json'),refs=require('../gesture-data/references.json');
const manifest=require('../gesture-data/manifest.json'),positive=manifest.filter(r=>r.wordId!=='__unknown__');
const name=id=>words.find(w=>w.id===id)?.kk||id,pct=n=>(n*100).toFixed(1)+'%';
const table=report=>['| Слово | Записей | Верно | Точность с отказами | Top-1 | Отказов | Ошибочные ответы |','|---|---:|---:|---:|---:|---:|---|',...report.byWord.map(r=>`| ${name(r.wordId)} | ${r.samples} | ${r.correct} | ${pct(r.accuracy)} | ${r.top1}/${r.samples} | ${r.rejected} | ${Object.entries(r.confusions).map(([id,n])=>name(id)+': '+n).join(', ')||'—'} |`)].join('\n');
const text=`# Проверка распознавания ${words.length} слов

## Что было причиной

Старый основной режим уже использовал последовательность RGB-кадров, а не отдельный снимок: Slovo MViTv2-32-2 с 1001 выходом. На диагностических 18 видео только 2 дали уверенный правильный результат. Отдельный персональный DTW привязывал запястья к координатам экрана и не учитывал плечи. Кроме того, одинаковые метки handedness двух рук приводили к перезаписи одного вектора.

Исправлены привязка к экрану, потеря второй руки, зависимость временных отсчётов от скорости камеры и отбрасывание короткого полного жеста из-за пауз с опущенными руками. Интерфейс сохранён; распознавание переведено в браузер.

## Архитектура

Камера → отсчёт 3 с → запись 2 с → MediaPipe Tasks Vision 0.10.21 (две руки + Pose Lite) → нормализация относительно запястья/ладони и плеч → интерполяция 40 кадров → направления движения → DTW в Web Worker → среднее лучших K эталонов каждого слова → проверка расстояния и отрыва от следующего слова.

Выбор видеообразца не передаётся распознавателю. Результат возникает после всей записи. Руки сравниваются и в зеркальном варианте, без изменения исходного изображения камеры. Короткие пропуски трекинга интерполируются; долго отсутствующая рука не выдумывается. Проверяются видимость плеч и количество кадров с руками. Число расстояния DTW не выдаётся за процент вероятности.

Размер ладони вычисляется в 3D: это уменьшает искажения при повороте кисти ребром к камере. Дополнительно проверено представление в локальных осях ладони; оно не улучшило общую обучающую проверку и имеет нулевой вес в итоговом решении.

Настройки: K=${refs.options.k}; минимальный относительный отрыв ${refs.options.minMargin}. Веса и пороги находятся в gesture-data/config.json. Они выбраны автоматически на обучающей части с исключением исполнителя; итоговая тестовая часть не использовалась для выбора весов и порогов. При расширении словаря веса признаков, K и минимальный отрыв сохранены; предел расстояния для каждого класса пересчитан по 90-му перцентилю верных ближайших ответов обучающей проверки.

## Данные

${manifest.length} исходных видео: 20 на каждое из ${words.length} слов и ${manifest.length-positive.length} отрицательных примера других слов. В рабочем файле ${refs.templates.length} пригодных эталонов; записи с плохим трекингом остаются в оценке как ошибки/отказы. В production включены все пригодные публичные эталоны, включая ролики из раздела «Как показать слово». Все отрицательные примеры исключены из эталонов.

gesture-data/manifest.json хранит исходные метки, идентификаторы роликов и исполнителей, официальный train/test. videos/ содержит исходные ролики; raw/ — извлечённые координаты; processed.json — нормированные последовательности; references.json — готовый кеш для браузера. Видеофайлы, инструменты извлечения и Python-библиотеки исключены из Vercel-сборки, кроме ${words.length} пользовательских видеообразцов.

Источник — [Slovo](https://github.com/hukenovs/slovo), **русский жестовый язык с казахским текстом на выходе**. Это не проверенная модель региональных жестов Казахстана. Исходная лицензия сохранена в vendor/slovo/LICENSE.pdf, атрибуция — vendor/slovo/ATTRIBUTION.md. Неаннотированное длинное старое демо examples/salem.mp4 не используется как обучающий эталон: достоверных границ слова у него нет.

## Официальная тестовая часть

Для каждого теста используются только train-эталоны и дополнительно исключается тот же исполнитель. Сам образец и все записи его исполнителя не могут оказаться ближайшими эталонами. Пять записей на слово — маленькая выборка. Часть видео ранее использовалась для диагностики проекта; это не новая слепая проверка пользователей. Точность на живой камере пользователя ещё не измерена.

Общая точность с учётом отказов: **${pct(held.accuracy)}**. Правильный top-1 до отказов: ${pct(held.top1)}. Неизвестные жесты отклонены: ${held.unknown.rejected}/${held.unknown.samples}. Медиана сравнения эталонов в Node на этом компьютере: ${held.medianMs} мс, без времени отсчёта, записи и MediaPipe.

${table(held)}

## Все записи с исключением исполнителя (LOSO)

Для каждого запроса исключаются он сам и все записи его исполнителя. Производственная база содержит больше эталонов, чем train-only проверка выше; здесь проверяется каждый из ${positive.length} положительных примеров. Веса/пороги всё ещё получены только на train. Оценка на данных, участвовавших в выборе настроек, может быть оптимистичной.

Общая точность: ${pct(loso.accuracy)}. Неизвестные отклонены: ${loso.unknown.rejected}/${loso.unknown.samples}.

${table(loso)}

Полные матрицы путаницы, причины отказов, топ-3 и идентификаторы примеров: gesture-data/evaluation-heldout.json и gesture-data/evaluation-loso.json. История выбора весов: gesture-data/tuning-v2.json, tuning.json и calibration.json. Сложные пары указаны в таблицах. Смена весов формы, положения, скорости, глубины, штрафа за отсутствующую руку и K проверена на обучающих исполнителях. Для дальнейшего улучшения нужны новые проверенные варианты жестов от нескольких людей; текущие показатели не означают надёжное распознавание произвольного исполнителя.

## Как добавлять примеры

Откройте http://127.0.0.1:3000/?dev=1#translator . Укажите код исполнителя (один и тот же для одного человека), выберите слово, нажмите «Записать пример». Цель — 20–30 записей на класс, затем 30–50 от нескольких людей. JSON сохраняет метку, raw-координаты, нормированную последовательность, число кадров, handedness, время и код исполнителя. Записи остаются в IndexedDB и не меняют рабочие эталоны до экспорта и импорта. Другие старые персональные базы не удаляются.

Нажмите «Скачать мою базу JSON», проверьте правильность меток и выполните:

\x60\x60\x60powershell
npm run samples:import -- "C:/path/qoldau-gesture-samples.json"
npm run references:build
npm run evaluate:loso
\x60\x60\x60

## Запуск и тесты

\x60\x60\x60powershell
npm start
# http://127.0.0.1:3000/#translator
npm ci
npm test
npm run evaluate
npm run evaluate:loso
\x60\x60\x60

Запуск сайта не требует Python и нейросетевого сервера. Для полного повторного извлечения исходных эталонов:

\x60\x60\x60powershell
python -m pip install --target .gesture-tools -r requirements-gestures.txt
python tools/fetch_gesture_data.py
python tools/extract_landmarks.py
npm run references:build
node tools/calibrate_gestures.cjs --fixed-weights
npm run references:build
npm run evaluate
npm run evaluate:loso
npm run examples:build
node tools/write_gesture_report.cjs
\x60\x60\x60

## Изменённые файлы и ограничения проверки

- app.js, index.html, style.css: камера, отсчёт, видеообразцы, локальный сбор данных и скрытая диагностика.
- landmark-recognition.js, recognition-worker.js: общие признаки, нормализация, интерполяция, DTW и отказ.
- tools/: загрузка, извлечение, импорт, сборка кеша, калибровка, оценка и этот отчёт.
- gesture-data/: исходная разметка, реальные координаты, кеш и результаты всех классов.
- server.cjs, package.json, vercel.json, .vercelignore: статический запуск и подготовка к Vercel. Старый Python-режим доступен только при QOLDAU_SLOVO=1 для сравнения.
- landmark-recognition.test.cjs, app.test.cjs, vocabulary.test.cjs: проверки признаков, зеркалирования, двух рук, отказов, DOM, отсчёта, захвата, отмены и режима разработки.

Пройдено 26 автоматических тестов, включая проверку импорта повреждённых записей. Проверены HTTP-ответы для страницы, скриптов, моделей, WASM, кеша эталонов и всех ${words.length} видеообразцов; API старого Python-режима отключён.

Проверки DOM используют реальные обработчики и DTW, но подставляют записанные MediaPipe-координаты вместо физической камеры. Работа на живой камере пользователя не проверена. Публикация на Vercel не выполнялась.
`;
fs.writeFileSync('GESTURE_REPORT.md',text);
console.log('Wrote GESTURE_REPORT.md');
