const fs=require('node:fs'),words=require('../data/words.json').words,refs=require('../gesture-data/references.json'),held=require('../gesture-data/evaluation-heldout.json'),manifest=require('../gesture-data/manifest.json');
const count=words.length,pct=(held.accuracy*100).toFixed(1),correct=held.byWord.reduce((sum,r)=>sum+r.correct,0);
let readme=fs.readFileSync('README.md','utf8');
readme=readme.replace(/переводчик (?:\d+|девяти) жестов/,'переводчик '+count+' жестов').replace(/со \d+ эталонами/,'со '+refs.templates.length+' эталонами');
readme=readme.replace(/На \d+ тестовых видео[^\n]+/,'На '+held.samples+' тестовых видео результат верный в '+correct+' случаях ('+pct+'%, отказы учитываются). Подробная точность каждого слова — в отчёте. Это прототип; точность на живой камере пока не измерена. Эталоны взяты из Slovo (РЖЯ), а не из проверенного корпуса жестового языка Казахстана.');
readme=readme.replace(/метрики всех (?:\d+|девяти) слов/,'метрики всех '+count+' слов').replace(/\d+ видеообразцов включены/,count+' видеообразцов включены').replace(/Все \d+ слов/g,'Все '+count+' слов').replace(/одно из \d+ слов/g,'одно из '+count+' слов');
fs.writeFileSync('README.md',readme);
let attribution=fs.readFileSync('vendor/slovo/ATTRIBUTION.md','utf8');
attribution=attribution.replace(/\d+ Kazakh text mappings/,count+' Kazakh text mappings').replace(/\d+ unchanged test videos/,count+' unchanged test videos');
attribution=attribution.replace(/\d+ source clips were selected[^\n]+/,manifest.length+' source clips were selected from the official Slovo archive: '+manifest.filter(r=>r.wordId!=='__unknown__').length+' clips across '+count+' words and '+manifest.filter(r=>r.wordId==='__unknown__').length+' negative examples. Source: https://rndml-team-cv.obs.ru-moscow-1.hc.sbercloud.ru/datasets/slovo/slovo.zip . Original clip IDs, labels, signers and train/test membership are preserved in gesture-data/manifest.json.');
attribution=attribution.replace(/\d+ usable positive clips/,refs.templates.length+' usable positive clips');fs.writeFileSync('vendor/slovo/ATTRIBUTION.md',attribution);
const guide=['# '+count+' слов и видеообразцы','','Все слова подключены к обоим режимам: «Жест → текст» и «Текст → жест». Источник — Slovo / РЖЯ с казахским переводом. Наличие эталонов не гарантирует правильное распознавание; результаты каждого слова приведены в GESTURE_REPORT.md.','','Откройте http://127.0.0.1:3000/#translator и выберите слово в нужном режиме. Видео можно замедлить и повторить.','','| Қазақша | По-русски | Видео |','|---|---|---|',...words.map(w=>`| ${w.kk} | ${w.ru} | [Посмотреть](http://127.0.0.1:3000/examples/words/${w.id}.webm) |`)];fs.writeFileSync('WORD_GUIDE.md',guide.join('\n')+'\n');
console.log('Documentation updated:',count,'words,',refs.templates.length,'references,',pct+'% heldout');
